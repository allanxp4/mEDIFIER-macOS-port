name: Build Qt

on:
  push:
    branches:
      - release/*

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      - name: Install Qt
        uses: jurplel/install-qt-action@v4.1.1
        with:
          version: 5.15.2
      - name: change dir 
        run: cd Qt
      - name: qmake
        run: qmake
        working-directory: repo/Qt
      - name: make
        run: make
        working-directory: repo/Qt
        
